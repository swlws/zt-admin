# 整体布局

## 页面来源

- 分析地址：`http://127.0.0.1:32767/start.html?id=tqy27f&p=数据看板`
- 原型工具：Axure 本地预览
- 当前页面：`数据看板`
- 页面 ID：`tqy27f`
- 生成时间：`2026-06-06T01:01:43.384Z`

## 站点菜单结构

```text
智庭科技售后管理系统
├── 封面
├── 项目概述
├── 业务流程
├── PC端
│   ├── 数据看板
│   ├── 工单管理
│   │   ├── 新建
│   │   └── 编辑
│   ├── 设备管理
│   ├── 工程师管理
│   ├── 客户管理
│   ├── 产品管理
│   └── 系统管理
├── 移动端-用户
│   ├── 登录页
│   ├── 首页
│   ├── 设备
│   ├── 报修
│   └── 我的
└── 移动端-工程师
    ├── 首页
    └── 页面 1
```

## 页面框架

页面是典型后台管理系统布局，画布尺寸约为 `1457 x 928`，主背景为浅灰色 `#F0F2F5`。

| 区域 | 位置/尺寸 | 说明 |
| --- | --- | --- |
| 页面背景 | `10,10 / 1457 x 928` | 承载整个后台页面，浅灰底色 |
| 左侧栏 | `10,10 / 196 x 918` | 深色固定侧边栏，包含品牌、导航和用户信息 |
| 顶部栏 | `206,10 / 1263 x 42` | 右侧内容区顶部工具栏，包含系统名、当前位置、搜索和用户入口 |
| 主内容区 | `206,52 / 1263 x 876` | 页面主体容器，内部内容可按页面类型纵向延展 |

## 顶部栏结构

顶部栏位于侧边栏右侧，横向铺满内容区。

| 元素 | 内容 | 说明 |
| --- | --- | --- |
| 系统标题 | 智庭科技售后系统 | 左侧展示当前系统名称 |
| 当前页面 | 数据看板 | 紧跟系统标题，表示当前模块 |
| 搜索框 | 全局搜索... | 右侧全局搜索入口 |
| 用户入口 | 管 | 右上角圆形/头像入口，展示管理员简称 |

## 侧边栏结构

侧边栏使用深蓝黑底色，宽度约 `196px`，分为品牌区、导航区、用户信息区。

### 品牌区

| 元素 | 内容 | 说明 |
| --- | --- | --- |
| Logo 图标 | 工具/服务类图标 | 蓝色圆角图标块 |
| 主品牌 | 智庭科技 | 品牌名称 |
| 副标题 | 售后管理系统 | 系统类型说明 |
| 折叠按钮 | 图标按钮 | 位于品牌区右侧，用于收起侧栏 |

### 导航区

| 菜单项 | 状态/附加信息 | 说明 |
| --- | --- | --- |
| 数据看板 | 当前选中 | 看板首页入口 |
| 工单管理 | Badge `14` | 工单列表与工单操作入口 |
| 设备管理 | 默认 | 设备资产管理入口 |
| 工程师管理 | 默认 | 工程师人员管理入口 |
| 客户管理 | 默认 | 客户信息管理入口 |
| 产品管理 | 默认 | 产品/型号管理入口 |
| 用户端预览 | 默认 | 用户端小程序或移动端预览入口 |
| 系统配置 | 默认 | 系统设置入口 |

### 用户信息区

位于侧边栏底部。

| 元素 | 内容 |
| --- | --- |
| 用户名 | 管理员 |
| 邮箱 | admin@zhiting.com |

## 实现拆分建议

整体布局可拆为稳定的后台框架组件，页面内容通过路由或插槽注入 `MainContent`。

```text
AdminLayout
├── Sidebar
│   ├── Brand
│   ├── NavMenu
│   └── UserProfile
├── Header
│   ├── ProductTitle
│   ├── PageTitle
│   ├── GlobalSearch
│   └── UserTrigger
└── MainContent
```

建议将侧边栏菜单作为独立配置维护：

```ts
const sidebarMenus = [
  { key: 'dashboard', label: '数据看板', active: true },
  { key: 'orders', label: '工单管理', badge: 14, children: ['新建', '编辑'] },
  { key: 'devices', label: '设备管理' },
  { key: 'engineers', label: '工程师管理' },
  { key: 'customers', label: '客户管理' },
  { key: 'products', label: '产品管理' },
  { key: 'mobilePreview', label: '用户端预览' },
  { key: 'settings', label: '系统配置' },
]
```
